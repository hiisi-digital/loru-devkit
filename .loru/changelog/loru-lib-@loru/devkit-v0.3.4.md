## @loru/devkit v0.3.4

- Sanitize changelog filenames for scoped package ids and ensure directories
  exist.
- Avoid reapplying stashed state after successful bump (drop stash instead).
- Add unit tests for changelog path safety.
